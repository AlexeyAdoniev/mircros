# Workspace Instructions

## Terminal Preferences

When running commands, always use the WSL terminal (not PowerShell). The workspace is located in the WSL filesystem at `/home/omaha/course2/jobber`.

## Additional Context

- This is a Node.js monorepo using Nx
- The project includes NestJS microservices (auth, executor, jobs)
- Commands should be executed in the WSL environment where the project is located
